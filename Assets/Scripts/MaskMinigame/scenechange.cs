using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

/*
public class scenechange : MonoBehaviour {

    public GameObject mask; 
	public GameObject choice; 
	public bool finalchoosed; 
	
	// Update is called once per frame
	void Update () {
		 choice = GameObject.Find("chooseholder"); // find the chooseholder object which has maskchoosing attached 
		 maskchoosing maskchoice = choice.GetComponent<maskchoosing>();
		 finalchoosed = maskchoice.choosed; 		 

		 if (finalchoosed == true) // if the flag in maskchoosing is on 
		 {
		    SceneManager.LoadScene(1, LoadSceneMode.Single);// load from mask choice to mask alignment 
		 }		
	}
}
*/